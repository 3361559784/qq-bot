# 🚫 GitHub Actions 存储已满，使用本地 CLI 部署
# 部署命令: func azure functionapp publish school-bot --build remote

name: Build and deploy Node.js project to Azure Function App - school-bot (DISABLED)

on:
  # 🚫 禁用自动触发，存储空间已满
  # push:
  #   branches:
  #     - main
  workflow_dispatch:  # 仅允许手动触发（但不建议使用）

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ GitHub Actions 存储已满 (2GB/2GB)"
          echo "请使用本地 CLI 部署："
          echo "  func azure functionapp publish school-bot --build remote"
          echo ""
          echo "或者清理 Actions 存储后重新启用此 workflow"
