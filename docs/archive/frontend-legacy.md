# Legacy Frontend Artifacts

The following files are archived and not used by the current standalone runtime:

- `docs/archive/deploy-frontend.sh`
- `docs/archive/next.config.mjs`

Active frontend implementation is under `frontend/`.
