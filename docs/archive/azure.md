# Azure Archive Notes

This repository has moved to a standalone runtime (Fastify + PostgreSQL).

Archived Azure-specific artifacts:

- Azure Functions deployment workflow (`.github/workflows/main_school-bot.yml`)
- Azure deployment script (`deploy-functions.sh`)

Legacy Azure function wrappers may still exist in source for short-term code reference, but production runtime no longer depends on Azure Functions or Cosmos DB.
