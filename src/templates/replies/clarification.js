/**
 * Clarification Templates - 澄清回复模板
 * 
 * 从 clarificationGate.js 导出的模板构建函数
 */

const { buildClarificationReply } = require('../../gates/clarificationGate');

module.exports = {
    buildClarificationReply
};
